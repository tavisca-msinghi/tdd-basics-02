using System;

namespace ConsoleCalculator
{
    public class Calculator
    {   
        private isvalid(char )

        public string SendKeyPress(char key)
        {
            // Add your implementation here.
            
            if(char.isdigit(key)){
                if(isvalid(key))
                int k=char.istodigit(key);
            
            }
            else{
            
            }
            throw new NotImplementedException();
        }
    }
}
